import { Header, NavBar, FeaturedProperty } from '../../Components';

export function HomePage(): JSX.Element {
    return (
        <>
            <NavBar />
            <Header />
            <FeaturedProperty />
        </>
    );
}